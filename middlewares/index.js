const ctrlWrapper = require('./ctrlWrapper');
const validation = require('./validation');
const authenticate = require('./authenticate');
const removeOwnerField = require('./removeOwnerField');

module.exports = {
    ctrlWrapper,
    validation,
    authenticate,
    removeOwnerField
}